#include <iostream>
#include <iomanip>
#include <vector>
#include <numeric>
#include <algorithm>
#include <set>
#include <map>
#include <string>
#include <queue>

void display_queue (std::queue <double> q) {
  std::cout << "[ ";
  while (! q.empty()) {
  auto elem = q.front();
  q.pop();
	std::cout << elem << " ";
	}
	std::cout << "]" << std::endl;
}

void display_queue(std::queue<std::string> q) {
	std::cout << "[ ";
	while (!q.empty()) {
		auto elem = q.front();
		q.pop();
		std::cout << elem << " ";
	}
	std::cout << "]" << std::endl;
}

int main() {

  
  return 0;
}
